# Shop

## Route

- `GET /shop` → `src/app/(site)/shop/page.tsx`

## Notes

- The shop page currently renders a static product list defined in the page module.
- Cart behavior is implemented via `CartContext` (used by `/cart`).
